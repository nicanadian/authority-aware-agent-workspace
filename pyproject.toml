[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "authority-aware-agent-workspace"
version = "0.1.0"
description = "Deterministic authority-aware agent workspace harness."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
